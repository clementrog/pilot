# Snapshots

Backups of /pilot files after successful commits.

After each commit + health PASS:
1. Current files copied here
2. Used for recovery via `restore` command

Empty until first successful commit.
